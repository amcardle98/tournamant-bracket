export class Player {
    /**
     * Unsure of what the player model needs, only putting name and score for now.
     * 
     */
    
    name: string;
    score: number;
    constructor(name: string) {
        this.name = name;
        this.score = 0;
    }

}
